# The portal uses the same GUC login as the CMS (Windows / NTLM).
# The example asks for it at runtime; you MAY set it here instead, but keep
# this file private and never commit real credentials.
# GUC_USERNAME=your.username
# GUC_PASSWORD=your.password
