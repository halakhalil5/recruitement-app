# Third-party dependencies. The guc_portal package is local (it sits in this
# folder), so it is imported directly and not listed here.
#   pip install -r requirements.txt
requests>=2.31
requests-ntlm>=1.2
beautifulsoup4>=4.12
lxml>=5.0
langchain>=1.0
langchain-anthropic>=0.3
python-dotenv>=1.0
jupyterlab>=4.0
ipykernel>=6.0
