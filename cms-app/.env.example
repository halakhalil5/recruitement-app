# Copy this to `.env` and fill in your key.
ANTHROPIC_API_KEY=sk-ant-...

# Your GUC login is asked for when the notebook runs (getpass, not saved).
# If you would rather not type it every time, you MAY put it here instead,
# but then keep this file private and never commit it.
# GUC_USERNAME=your.username
# GUC_PASSWORD=your.password
